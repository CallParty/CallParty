import React from 'react'
import { NewCampaignAction } from './NewCampaignAction'

const NewCampaignCall = (props) => (
  <NewCampaignAction actionType="CampaignCall" {...props} />
)

export default NewCampaignCall